//setAccessTokenCookie
//check if waitTime has elapsed
//add track to spotifyPlaylist

const addTrack = (spotifyUserId, musicSessionId, spotifyAccessToken) => {
    if (!spotifyAccessToken) {
        
    }
};

module.exports = { addTrack };
